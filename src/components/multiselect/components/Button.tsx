import React from 'react';

// styles
import classes from './Button.module.css';

const Button = () => {
  return <div className={classes.select}>-- Select Your Todo --</div>;
};

export default Button;
